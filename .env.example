# Copy to .env and tweak for your local stack.

# NestJS API
PORT=3333
MONGODB_URI=mongodb://localhost:27017/hand-betting-game

# Frontend (build-time only — production swaps environment.prod.ts)
WEB_API_URL=http://localhost:3333/api
